"use Strict";

function firstfunction(Name) {
    return "Hello " + name;

    const firstArrowfunction = (name) => "Hello";

    console.log(firstfunction(name "Miguel"));
    console.log(firstfunction(name "Diana"));

    // Parsing my stuff in a function
function parseMyStuff(input) {
    return parseFloat(input)

}


console.log(price);

let maybeString = "I am a string"{
    typeof maybeString;
}

console.log(isString(maybeString));

function isNumber(input) {

}
    return typeof input === "number";

console.log(isNumber(input"4"));


    const increment = function(x) {
        return x + 1;
    };

    console.log(increment(1);
}